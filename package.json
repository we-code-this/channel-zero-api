{
  "name": "channelzero-api",
  "version": "1.0.0",
  "description": "package for creating workers templates",
  "main": "index.js",
  "scripts": {
    "migrate": "node migrate up",
    "rollback": "node migrate down",
    "build": "webpack",
    "seed": "",
    "test": "npm run build && NODE_ENV=test npm run rollback && NODE_ENV=test npm run migrate && NODE_ENV=test npm run seed && NODE_ENV=test nyc --reporter=lcov --all mocha --timeout 5000",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Mike Gregoire <mgregoire@nvzion.com>",
  "license": "ISC",
  "devDependencies": {
    "@dollarshaveclub/cloudworker": "^0.1.2",
    "better-sqlite3": "^7.1.0",
    "chai": "^4.2.0",
    "chai-date-string": "^0.1.0",
    "dayjs": "^1.8.31",
    "mocha": "^8.0.1",
    "nyc": "^15.1.0",
    "prettier": "^1.17.0",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "dotenv": "^8.2.0",
    "fs-extra": "^9.0.1",
    "serverless-cloudflare-workers": "^1.2.0",
    "serverless-mysql": "^1.5.4",
    "webpack": "^4.44.0"
  }
}
